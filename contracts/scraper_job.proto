syntax = "proto3";

package contracts;

service URLQueue {
    rpc Dequeue(Empty) returns (ScraperJob) {}
    rpc Enqueue(ScraperJob) returns (Empty) {}
}

message ScraperJob {
    string url = 1;
    bool requeue = 2;
}

message Empty {

}